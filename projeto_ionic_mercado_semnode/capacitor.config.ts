import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'projeto_ionic_mercado',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
